# python
export PYTHONSTARTUP="${HOME}/.python"

# perl
export PERLLIB="/opt/local/lib/perl5/site_perl/5.8.8/"

# X11
#export DISPLAY=:0.0

# i18n
# set papersize to A4
#export LANG="nl_NL@euro.UTF-8"

# these breaks man pages (see issue #6) and it doesn't seems to be required,
# let's not use them for now
#export LANG="nl_NL.UTF-8"
export LC_MESSAGES="en_US.UTF-8"

# bc
if [[ -f /usr/bin/bc ]] ; then
	export BC_ENV_ARGS=" -q"
fi

# Enable colors for ls, etc.  Prefer ~/.dir_colors #64489
if [[ -f ~/.dir_colors ]] ; then
	eval $(dircolors -b ~/.dir_colors)
elif [[ -f /etc/DIR_COLORS ]] ; then
	eval $(dircolors -b /etc/DIR_COLORS)
fi

export EDITOR=vim

# for KeePass 2.x
export MONO_MWF_MAC_FORCE_X11=
