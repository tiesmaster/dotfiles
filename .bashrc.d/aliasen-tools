alias stratum='ntpq -c rv |grep stratum'
alias cputemp='sensors |grep CPU'

#alias wakeupsaikox='wakeonlan 00:30:1B:AF:01:9F'

alias xalan="java org.apache.xalan.xslt.Process"
# this doesn't work (it keeps borking about a 127 error, however, adding this
# alias, even when Xalan (or even Java) isn't present on the system, doesn't
# break things, so keep it
#if [ ! `xalan </dev/zero >/dev/null 2>&1` ]; then
#	unalias xalan
#	echo "alias xalan: XSLT parser Xalan was not found, please install and set in the CLASSPATH"
#fi
